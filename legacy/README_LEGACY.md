Legacy scripts kept for reference.

These files are not the primary path for the current benchmark workflow:

- `generate_queries.py`
- `visualize_tasks.py`
- `run_spatial_agent.py`

Current active entry points remain at the top level of `cherry/`, especially:

- `run_query_benchmark.py`
- `analyze_query_results.py`
- `run_embodied_eval.py`

Batch shell helpers now live in `scripts/`.
